<form method="post" action="{{ path('app_watch_history_delete', {'id': watch_history.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ watch_history.id) }}">
    <button class="btn">Delete</button>
</form>
