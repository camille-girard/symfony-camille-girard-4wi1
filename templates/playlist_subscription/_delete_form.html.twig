<form method="post" action="{{ path('app_playlist_subscription_delete', {'id': playlist_subscription.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ playlist_subscription.id) }}">
    <button class="btn">Delete</button>
</form>
